## Домашнее задание к лекции 2 «Docker», nginx контейнер
#### студент: Сергей Романов

#### группа: WebPY-1
____
команда для создания образа
```
docker build -t nginx .
```

команда для запуска контейнера
```
docker run -p 8080:8080 --name some-nginx --rm nginx
```
